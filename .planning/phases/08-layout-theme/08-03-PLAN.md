---
phase: 08-layout-theme
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/components/commitment/CommitmentPanel.tsx
  - src/components/commitment/CommitmentCard.tsx
  - src/components/commitment/TensionAlertItem.tsx
  - src/components/commitment/GoalSpaceSection.tsx
  - src/components/commitment/TrajectoryBadge.tsx
  - src/components/commitment/AllocationSummary.tsx
  - src/components/shared/NodeTypeBadge.tsx
  - src/components/shared/StatusBadge.tsx
  - src/components/shared/EmptyState.tsx
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-03
must_haves:
  truths:
    - "CommitmentPanel sidebar is readable in both light and dark mode"
    - "Tension alert backgrounds have dark mode variants — no hardcoded dark-only red/amber backgrounds"
    - "Shared badge components (NodeTypeBadge, StatusBadge) are readable in both modes"
    - "No component in commitment/ or shared/ has hardcoded dark-only Tailwind classes"
  artifacts:
    - path: "src/components/commitment/CommitmentPanel.tsx"
      provides: "Panel with dark: variants on all bg/border/text"
      contains: "dark:bg-gray-950\|dark:border-gray-800"
    - path: "src/components/commitment/TensionAlertItem.tsx"
      provides: "Tension alerts with light+dark background variants"
      contains: "dark:bg-red-950\|dark:bg-amber-950"
    - path: "src/components/shared/NodeTypeBadge.tsx"
      provides: "Badge readable in light and dark"
      contains: "dark:"
  key_links:
    - from: "src/components/commitment/CommitmentPanel.tsx"
      to: "tailwind dark: variants"
      via: "dark: prefix on all bg/border/text classes"
      pattern: "dark:"
    - from: "src/components/commitment/TensionAlertItem.tsx"
      to: "SEVERITY_STYLES constant"
      via: "Updated with light+dark background classes"
      pattern: "SEVERITY_STYLES"
---

<objective>
Audit and fix all commitment panel and shared badge components for light/dark mode. The commitment panel (left sidebar), its child cards, tension alerts, and all shared badge components must be readable in both modes. Runs in parallel with 08-02.

Purpose: Commitment panel sidebar and tension alerts must match the main app theme in both light and dark mode (LAYOUT-02, LAYOUT-03).
Output: All files in src/components/commitment/ and src/components/shared/ use light/dark adaptive colors.
</objective>

<execution_context>
@/Users/gurden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gurden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-layout-theme/08-01-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key current state extracted from codebase. Executor MUST read actual files before editing. -->

CommitmentPanel.tsx — hardcoded dark-only colors:
- Collapsed state container: `absolute left-0 top-[49px] bottom-0 w-6 ... bg-gray-950 border-r border-gray-800/50`
- Expanded container: `absolute left-0 top-[49px] bottom-0 w-[260px] bg-gray-950 border-r border-gray-800/50`
- Header: `border-b border-gray-800/50`
- Section label: `text-gray-500`, collapse button: `text-gray-600 hover:text-gray-400`
- Add button: `text-gray-600 hover:text-gray-400 border border-dashed border-gray-800 hover:border-gray-700`

TensionAlertItem.tsx — SEVERITY_STYLES (lines 13-17):
```typescript
const SEVERITY_STYLES: Record<string, string> = {
  high:   'bg-red-950/60 border border-red-900/50',
  medium: 'bg-amber-950/60 border border-amber-900/50',
  low:    'bg-gray-900 border border-gray-800',
};
```
These need light mode equivalents. SEVERITY_TEXT colors (text-red-400, text-amber-400, text-gray-500) are semantic and readable in both modes — leave them.

The resolve modal (lines 93-152): `bg-black/70` overlay, `bg-gray-900 border-gray-700`, textarea `bg-gray-800 border-gray-700 text-gray-200` — all need dark: variants.

CommitmentCard.tsx — read file and apply same dark: pattern.
GoalSpaceSection.tsx — read file and apply same dark: pattern.
TrajectoryBadge.tsx — read file and apply same dark: pattern.
AllocationSummary.tsx — read file and apply same dark: pattern.

NodeTypeBadge.tsx — uses node type colors (same in both modes), but surrounding UI elements may have hardcoded dark values.
StatusBadge.tsx — read file and audit.
EmptyState.tsx — likely has text-gray-500 etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix CommitmentPanel and CommitmentCard for light/dark mode</name>
  <files>src/components/commitment/CommitmentPanel.tsx, src/components/commitment/CommitmentCard.tsx</files>
  <read_first>
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/CommitmentPanel.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/CommitmentCard.tsx
  </read_first>
  <action>
**CommitmentPanel.tsx:**

Collapsed container:
`absolute left-0 top-[49px] bottom-0 w-6 flex flex-col items-center pt-3 bg-gray-950 border-r border-gray-800/50 z-10`
→ `absolute left-0 top-[49px] bottom-0 w-6 flex flex-col items-center pt-3 bg-white dark:bg-gray-950 border-r border-gray-200/80 dark:border-gray-800/50 z-10`

Expanded container:
`absolute left-0 top-[49px] bottom-0 w-[260px] bg-gray-950 border-r border-gray-800/50 z-10 flex flex-col overflow-hidden`
→ `absolute left-0 top-[49px] bottom-0 w-[260px] bg-white dark:bg-gray-950 border-r border-gray-200/80 dark:border-gray-800/50 z-10 flex flex-col overflow-hidden`

Header border: `border-b border-gray-800/50` → `border-b border-gray-200/80 dark:border-gray-800/50`

Section label `text-gray-500 uppercase tracking-wider` → `text-gray-400 dark:text-gray-500 uppercase tracking-wider`

Collapse/expand buttons `text-gray-600 hover:text-gray-400` → `text-gray-400 hover:text-gray-600 dark:text-gray-600 dark:hover:text-gray-400`

"Unlinked" label `text-gray-600 uppercase` → `text-gray-500 dark:text-gray-600 uppercase`

Add commitment button:
`text-gray-600 hover:text-gray-400 border border-dashed border-gray-800 hover:border-gray-700`
→ `text-gray-500 hover:text-gray-700 dark:text-gray-600 dark:hover:text-gray-400 border border-dashed border-gray-300 hover:border-gray-400 dark:border-gray-800 dark:hover:border-gray-700`

Tension section border `border-t border-gray-800/50` → `border-t border-gray-200/80 dark:border-gray-800/50`

Tensions label `text-gray-600 uppercase` → `text-gray-500 dark:text-gray-600 uppercase`

Tension dot indicator `bg-amber-500` — keep as-is (semantic color).

"No commitments" empty text `text-gray-600 italic` → `text-gray-500 dark:text-gray-600 italic`

**CommitmentCard.tsx:**

Read the entire file. Apply the same pattern:
- Card container backgrounds: `bg-gray-900` → `bg-gray-50 dark:bg-gray-900`; `bg-gray-800` → `bg-gray-100 dark:bg-gray-800`
- Borders: `border-gray-800` → `border-gray-200 dark:border-gray-800`; `border-gray-700` → `border-gray-200 dark:border-gray-700`
- Title text `text-gray-300` or `text-gray-200` → `text-gray-800 dark:text-gray-300` (or appropriate light equivalent)
- Label text `text-gray-500` → `text-gray-600 dark:text-gray-500`
- Muted text `text-gray-600` → `text-gray-500 dark:text-gray-600`
- STATUS_COLORS: `text-emerald-400`, `text-amber-400`, `text-gray-500`, `text-gray-600` — these semantic colors are readable in both modes. Add light equivalents only if contrast is insufficient on white: `text-emerald-600 dark:text-emerald-400`, `text-amber-600 dark:text-amber-400`
- Selected state border/ring colors — read from file and apply same dark: pairing
  </action>
  <verify>
    <automated>grep -n "dark:bg-gray\|dark:border-gray\|dark:text-gray" /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/CommitmentPanel.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/CommitmentCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "dark:bg-gray" CommitmentPanel.tsx returns at least 2 matches (collapsed + expanded containers)
    - grep "dark:border-gray" CommitmentPanel.tsx returns at least 2 matches
    - grep "bg-gray-950" CommitmentPanel.tsx returns zero unadorned matches (all must have dark: prefix or light equivalent before them)
    - grep "dark:bg-gray\|dark:border-gray" CommitmentCard.tsx returns at least one match
  </acceptance_criteria>
  <done>CommitmentPanel and CommitmentCard are readable in both light mode (white bg, dark text) and dark mode (gray-950 bg, light text). The sidebar adapts cleanly to both modes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix TensionAlertItem — light/dark severity backgrounds and resolve modal</name>
  <files>src/components/commitment/TensionAlertItem.tsx</files>
  <read_first>
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/TensionAlertItem.tsx
  </read_first>
  <action>
**SEVERITY_STYLES constant — update with light+dark pairs:**

Current:
```typescript
const SEVERITY_STYLES: Record<string, string> = {
  high:   'bg-red-950/60 border border-red-900/50',
  medium: 'bg-amber-950/60 border border-amber-900/50',
  low:    'bg-gray-900 border border-gray-800',
};
```

Replace with (light mode gets softer tinted backgrounds):
```typescript
const SEVERITY_STYLES: Record<string, string> = {
  high:   'bg-red-50 border border-red-200 dark:bg-red-950/60 dark:border-red-900/50',
  medium: 'bg-amber-50 border border-amber-200 dark:bg-amber-950/60 dark:border-amber-900/50',
  low:    'bg-gray-50 border border-gray-200 dark:bg-gray-900 dark:border-gray-800',
};
```

**SEVERITY_TEXT constant — update for light mode contrast:**

Current:
```typescript
const SEVERITY_TEXT: Record<string, string> = {
  high:   'text-red-400',
  medium: 'text-amber-400',
  low:    'text-gray-500',
};
```

These are too light for a white background. Replace with:
```typescript
const SEVERITY_TEXT: Record<string, string> = {
  high:   'text-red-600 dark:text-red-400',
  medium: 'text-amber-600 dark:text-amber-400',
  low:    'text-gray-500 dark:text-gray-500',
};
```

**Alert body text:**

`text-gray-400` (description paragraph) → `text-gray-600 dark:text-gray-400`

Acknowledge/Resolve button links `text-gray-500 hover:text-gray-300` → `text-gray-500 hover:text-gray-700 dark:text-gray-500 dark:hover:text-gray-300`

**Resolve modal:**

Modal overlay `bg-black/70` → `bg-black/50 dark:bg-black/70` (slightly lighter on light mode)

Modal container `bg-gray-900 border border-gray-700` → `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700`

Modal title `text-gray-200` → `text-gray-900 dark:text-gray-200`

Modal description `text-gray-500` → `text-gray-600 dark:text-gray-500`

"What do you now believe?" label `text-gray-500 uppercase` → `text-gray-500 dark:text-gray-500`

Textarea `bg-gray-800 border-gray-700 text-gray-200` → `bg-gray-50 dark:bg-gray-800 border-gray-200 dark:border-gray-700 text-gray-900 dark:text-gray-200`
Textarea focus: `focus:border-gray-600` → `focus:border-gray-400 dark:focus:border-gray-600`
Textarea placeholder color — no change needed (browser default handles this)

"Action" label `text-gray-500` → unchanged (same in both modes)

Radio option text `text-gray-400` → `text-gray-700 dark:text-gray-400`

Cancel button `text-gray-500 hover:text-gray-300` → `text-gray-500 hover:text-gray-700 dark:text-gray-500 dark:hover:text-gray-300`

Resolve button `bg-gray-700 hover:bg-gray-600 text-gray-200` → `bg-gray-100 hover:bg-gray-200 text-gray-800 dark:bg-gray-700 dark:hover:bg-gray-600 dark:text-gray-200`
  </action>
  <verify>
    <automated>grep -n "dark:bg-red-950\|dark:bg-amber-950\|dark:bg-gray-900\|dark:bg-gray-800\|dark:bg-white\|dark:text-gray" /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/TensionAlertItem.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "dark:bg-red-950" TensionAlertItem.tsx returns one match (high severity SEVERITY_STYLES)
    - grep "dark:bg-amber-950" TensionAlertItem.tsx returns one match (medium severity SEVERITY_STYLES)
    - grep "dark:bg-gray-900" TensionAlertItem.tsx returns one match (low severity SEVERITY_STYLES)
    - grep "bg-white dark:bg-gray-900\|bg-white.*dark:bg-gray-900" TensionAlertItem.tsx returns one match (resolve modal container)
    - grep "bg-red-950/60\|bg-amber-950/60" TensionAlertItem.tsx returns zero unadorned matches (all replaced with light+dark pairs)
  </acceptance_criteria>
  <done>Tension alert items show tinted light backgrounds (red-50, amber-50) in light mode and the original dark tints in dark mode. The resolve modal is readable in both modes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix GoalSpaceSection, TrajectoryBadge, AllocationSummary, and shared badge components</name>
  <files>
    src/components/commitment/GoalSpaceSection.tsx,
    src/components/commitment/TrajectoryBadge.tsx,
    src/components/commitment/AllocationSummary.tsx,
    src/components/shared/NodeTypeBadge.tsx,
    src/components/shared/StatusBadge.tsx,
    src/components/shared/EmptyState.tsx
  </files>
  <read_first>
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/GoalSpaceSection.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/TrajectoryBadge.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/AllocationSummary.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/shared/NodeTypeBadge.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/shared/StatusBadge.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/shared/EmptyState.tsx
  </read_first>
  <action>
Read each file fully before editing. Apply the following transformation rules consistently:

**Color mapping rules (apply to every file in this task):**

| Current (dark-only) | Light mode | Dark mode (keep) |
|---------------------|-----------|-----------------|
| `bg-gray-950` | `bg-white` | `dark:bg-gray-950` |
| `bg-gray-900` | `bg-gray-50` | `dark:bg-gray-900` |
| `bg-gray-800` | `bg-gray-100` | `dark:bg-gray-800` |
| `bg-gray-700` | `bg-gray-200` | `dark:bg-gray-700` |
| `border-gray-800` | `border-gray-200` | `dark:border-gray-800` |
| `border-gray-700` | `border-gray-200` | `dark:border-gray-700` |
| `text-gray-200` | `text-gray-800` | `dark:text-gray-200` |
| `text-gray-300` | `text-gray-700` | `dark:text-gray-300` |
| `text-gray-400` | `text-gray-600` | `dark:text-gray-400` |
| `text-gray-500` | `text-gray-500` | (same in both — skip) |
| `text-gray-600` | `text-gray-500` | `dark:text-gray-600` |

**Exceptions — do NOT change these (same in both modes per user spec):**
- Node type colors: `text-node-hunch`, `text-node-commitment`, etc.
- Semantic status colors: `text-emerald-400`, `text-amber-400`, `text-red-400`, `text-teal-400`, `text-yellow-500`
- TrajectoryBadge converging/neutral/drifting/pending color classes — these are semantic indicators, keep as-is
- Any `style={{ color: '...' }}` inline with node type hex colors — keep as-is

**GoalSpaceSection.tsx:**
Apply table above. Special attention to section headers, goal space name text, expand/collapse buttons, and any nested card containers.

**TrajectoryBadge.tsx:**
Apply table above to container backgrounds and borders only. The badge color classes for trajectory status (converging = green, drifting = red, neutral = gray, pending = gray) are semantic — do not change their text colors, only the surrounding container if it has hardcoded dark backgrounds.

**AllocationSummary.tsx:**
Apply table above. Any progress bars or allocation indicators: keep the colored fill (it uses node-type colors), apply light/dark to the track/background of the bar.

**NodeTypeBadge.tsx:**
Node type badges use `text-{nodeTypeColor}` and often `bg-{nodeTypeColor}/20`. The `/20` opacity keeps them legible on both light and dark backgrounds — leave these. Only update surrounding container backgrounds if any exist.

**StatusBadge.tsx:**
Read and apply the table above. Status colors (promoted, archived, llm_reviewed, etc.) often use semantic tints — keep the color hue, but add light mode versions if the background is too dark for a white page.

**EmptyState.tsx:**
Apply table above. Text and icon colors updated for light mode visibility.
  </action>
  <verify>
    <automated>grep -rn "dark:bg-gray\|dark:border-gray\|dark:text-gray" /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/GoalSpaceSection.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/commitment/TrajectoryBadge.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/shared/NodeTypeBadge.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/shared/EmptyState.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "dark:" GoalSpaceSection.tsx returns at least 3 matches (multiple color replacements)
    - grep "dark:" TrajectoryBadge.tsx returns at least one match
    - grep "dark:" NodeTypeBadge.tsx returns at least one match OR the file contains only node-type colors that require no change (check the actual content — if it uses only bg-{nodeType}/20 and text-{nodeType}, no dark: needed and this criterion is satisfied)
    - grep "dark:" EmptyState.tsx returns at least one match
    - grep "dark:" AllocationSummary.tsx returns at least one match
    - No file in src/components/commitment/ or src/components/shared/ contains an unadorned `bg-gray-950` or `bg-gray-900` that is not paired with a light-mode class
  </acceptance_criteria>
  <done>GoalSpaceSection, TrajectoryBadge, AllocationSummary, NodeTypeBadge, StatusBadge, and EmptyState all use dark: variants. Every component in commitment/ and shared/ is readable in both light and dark mode.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "dark:bg-gray" src/components/commitment/CommitmentPanel.tsx` — returns matches
2. `grep "dark:bg-red-950" src/components/commitment/TensionAlertItem.tsx` — returns one match
3. `grep "dark:bg-amber-950" src/components/commitment/TensionAlertItem.tsx` — returns one match
4. `grep "dark:" src/components/commitment/GoalSpaceSection.tsx` — returns at least 3 matches
5. `grep "bg-white dark:bg-gray-900" src/components/commitment/TensionAlertItem.tsx` — returns one match (modal)
</verification>

<success_criteria>
- CommitmentPanel sidebar matches the main app theme in both light (white bg) and dark mode (gray-950 bg)
- Tension alerts use light-tinted backgrounds in light mode (red-50, amber-50) and dark tints in dark mode
- Resolve modal is readable in both modes with white/dark-900 adaptive background
- All shared badges (NodeTypeBadge, StatusBadge, EmptyState) are readable in both modes
- No hardcoded dark-only colors remain in src/components/commitment/ or src/components/shared/
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-theme/08-03-SUMMARY.md`
</output>
