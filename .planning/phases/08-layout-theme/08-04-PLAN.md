---
phase: 08-layout-theme
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/review/page.tsx
  - src/app/settings/page.tsx
  - src/app/capture/page.tsx
  - src/app/reflect/ReflectClient.tsx
  - src/components/review/ReviewCard.tsx
  - src/components/graph/InlineCaptureCard.tsx
autonomous: true
requirements:
  - LAYOUT-02
gap_closure: true
must_haves:
  truths:
    - "The review page list cards are readable in both light and dark mode — no hardcoded dark-only backgrounds"
    - "The settings page type cards are readable in both light and dark mode"
    - "The capture page headings are readable in both light and dark mode"
    - "ReflectClient trajectory cards, textareas, and decision inputs use dark: variants"
    - "ReviewCard connection section border and action buttons use dark: variants"
    - "InlineCaptureCard overlay, inputs, and selects use dark: variants"
  artifacts:
    - path: "src/app/review/page.tsx"
      provides: "Review page with dark: variants on all list-card bg/border/text"
      contains: "dark:bg-gray-900\|dark:border-gray-800"
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with dark: variants on type-card rows"
      contains: "dark:bg-gray-900\|dark:border-gray-800"
    - path: "src/app/capture/page.tsx"
      provides: "Capture page headings with dark: text variants"
      contains: "dark:text-gray-200\|dark:text-gray-400"
    - path: "src/app/reflect/ReflectClient.tsx"
      provides: "ReflectClient with dark: variants on all interactive elements"
      contains: "dark:bg-gray-800\|dark:border-gray-600"
    - path: "src/components/review/ReviewCard.tsx"
      provides: "ReviewCard border and connection section with dark: variants"
      contains: "dark:border-gray-800"
    - path: "src/components/graph/InlineCaptureCard.tsx"
      provides: "InlineCaptureCard with dark: variants on card, inputs, selects"
      contains: "dark:bg-gray-900\|dark:border-gray-700"
  key_links:
    - from: "src/app/review/page.tsx"
      to: "tailwind dark: variants"
      via: "SEVERITY_COLORS constant and all card className strings updated with light+dark pairs"
      pattern: "dark:bg-gray-900"
    - from: "src/app/reflect/ReflectClient.tsx"
      to: "tailwind dark: variants"
      via: "windowButtonClass helper and all hardcoded bg-gray-7xx/8xx replaced with light+dark pairs"
      pattern: "dark:bg-gray-[78]"
    - from: "src/components/graph/InlineCaptureCard.tsx"
      to: "tailwind dark: variants"
      via: "Card root div and all input/select className strings updated"
      pattern: "dark:bg-gray-9"
---

<objective>
Apply Tailwind dark: variants to the six remaining files that still use hardcoded dark-only Tailwind classes: the review, settings, and capture pages, the ReflectClient component, ReviewCard, and InlineCaptureCard. This closes the LAYOUT-02 gap — after this plan, no hardcoded dark-only gray backgrounds or text colors remain anywhere in the app.

Purpose: Complete light/dark mode coverage so LAYOUT-02 ("no hardcoded colors remaining") is fully satisfied.
Output: Six updated files where every gray-9xx/8xx/7xx color is preceded by a light-mode equivalent using Tailwind dark: variants.
</objective>

<execution_context>
@/Users/gurden/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gurden/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-layout-theme/08-01-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current state extracted from the six files. Executor MUST read each file before editing. -->

Color mapping rules (same table used in 08-02 and 08-03 — apply consistently):

| Current (dark-only)  | Light mode       | Combined result                              |
|----------------------|------------------|----------------------------------------------|
| bg-gray-950          | bg-white         | bg-white dark:bg-gray-950                    |
| bg-gray-900          | bg-gray-50       | bg-gray-50 dark:bg-gray-900                  |
| bg-gray-800          | bg-gray-100      | bg-gray-100 dark:bg-gray-800                 |
| bg-gray-700          | bg-gray-200      | bg-gray-200 dark:bg-gray-700                 |
| bg-gray-800/50       | bg-gray-100/50   | bg-gray-100/50 dark:bg-gray-800/50           |
| border-gray-800      | border-gray-200  | border-gray-200 dark:border-gray-800         |
| border-gray-700      | border-gray-200  | border-gray-200 dark:border-gray-700         |
| border-gray-600      | border-gray-300  | border-gray-300 dark:border-gray-600         |
| text-gray-200        | text-gray-800    | text-gray-800 dark:text-gray-200             |
| text-gray-300        | text-gray-700    | text-gray-700 dark:text-gray-300             |
| text-gray-400        | text-gray-600    | text-gray-600 dark:text-gray-400             |
| text-gray-500        | text-gray-500    | (same — skip unless contrast is wrong)       |
| text-gray-600        | text-gray-500    | text-gray-500 dark:text-gray-600             |
| placeholder-gray-600 | placeholder-gray-400 | placeholder-gray-400 dark:placeholder-gray-600 |

Exceptions — do NOT change (semantic colors, same in both modes):
- Node type colors: text-node-hunch, bg-node-hunch, text-node-assumption-*, bg-node-assumption-*
- Semantic alert colors: text-amber-500, text-red-400, text-green-400, text-teal-400, border-amber-900/30
- The bg-teal-600 save button in ReflectClient — semantic action color, same in both modes
- Inline styles: style={{ backgroundColor: type.color }} — leave as-is (already dynamic)
- The [#085041] text color for goal space labels in InlineCaptureCard — leave as-is (brand color)
- The bg-[#185FA5] commitment stripe and bg-[#D4537E] test dot in review/page.tsx — leave as-is

Key hardcoded classes found per file:

src/app/review/page.tsx:
- h1: text-gray-200
- list cards (awaiting/lowConf/tests/stalled/commitments): bg-gray-900 border border-gray-800
- card text: text-gray-200, text-gray-300, text-gray-400, text-gray-600
- tension alert container: bg-gray-900 (in JSX at line 256)
- SEVERITY_COLORS constant (lines 123-127): text-red-400/text-amber-400/text-gray-500 for text,
  border-red-900/50 / border-amber-900/50 / border-gray-800 for borders
  NOTE: The SEVERITY_COLORS text values (text-red-400, text-amber-400) are semantic — leave those.
  Update only border-gray-800 → border-gray-200 dark:border-gray-800 in the low entry.
  The bg-gray-900 at line 256 needs a light equivalent.

src/app/settings/page.tsx:
- h1: text-gray-200
- type-row cards: bg-gray-900 border border-gray-800, text-gray-200, text-gray-600
- agent config section: bg-gray-900 border border-gray-800, text-gray-300, text-gray-500

src/app/capture/page.tsx:
- h1: text-gray-200
- h2: text-gray-400

src/app/reflect/ReflectClient.tsx:
- windowButtonClass inactive state: bg-gray-700 text-gray-300 hover:bg-gray-600
- goal space cards: bg-gray-800 (line 113)
- card text: text-gray-200, text-gray-400, text-gray-100
- reflection question label: text-gray-300
- textareas: bg-gray-800 border border-gray-600 text-gray-100
- decision list items: bg-gray-800, text-gray-200
- decision input: bg-gray-800 border border-gray-600 text-gray-100
- add decision button: bg-gray-700 text-gray-300

src/components/review/ReviewCard.tsx:
- "Suggested Connections" label: text-gray-400
- "No connections suggested" text: text-gray-600
- Action divider: border-t border-gray-800

src/components/graph/InlineCaptureCard.tsx:
- Card root: bg-gray-900 border border-gray-700 (line 131)
- "New node" label: text-gray-500 (same in both — leave)
- Title input: bg-gray-800 border border-gray-700, text-gray-200, placeholder-gray-600 (line 144)
- Select (node type): bg-gray-800 border border-gray-700, text-gray-400 (line 150)
- Goal space section container: bg-gray-800/50 border border-gray-700/50 (lines 158, 180)
- Goal space label: text-gray-600 normal-case — text-gray-600 → text-gray-500 dark:text-gray-600
- Goal space select: bg-gray-800 border border-gray-700, text-gray-300 (line 168)
- Outcome select: bg-gray-800 border border-gray-700, text-gray-300 (line 189)
- Expected signals input: bg-gray-800 border border-gray-700, text-gray-200, placeholder-gray-600 (line 207)
- Cancel button: text-gray-500 hover:text-gray-300 → text-gray-500 hover:text-gray-700 dark:hover:text-gray-300
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dark: variants to review/page.tsx, settings/page.tsx, and capture/page.tsx</name>
  <files>src/app/review/page.tsx, src/app/settings/page.tsx, src/app/capture/page.tsx</files>
  <read_first>
    - /Users/gurden/Documents/code/cof-learning-system/src/app/review/page.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/app/settings/page.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/app/capture/page.tsx
  </read_first>
  <action>
Apply the color mapping table from the interfaces block to each file.

**src/app/review/page.tsx:**

1. Page title h1: `text-gray-200` → `text-gray-800 dark:text-gray-200`

2. Section headers h2: `text-gray-500` — leave as-is (same in both modes)

3. List card links and divs (awaiting, lowConf, tests, stalled, commitments):
   `bg-gray-900 border border-gray-800` → `bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-800`
   `hover:border-gray-700` → `hover:border-gray-300 dark:hover:border-gray-700`

4. Card text inside those rows:
   `text-gray-200` → `text-gray-800 dark:text-gray-200`
   `text-gray-300` → `text-gray-700 dark:text-gray-300`
   `text-gray-400` → `text-gray-600 dark:text-gray-400`
   `text-gray-600` → `text-gray-500 dark:text-gray-600`

5. SEVERITY_COLORS constant (lines 123-127) — update the `low` entry border only:
   ```typescript
   const SEVERITY_COLORS: Record<string, string> = {
     high:   'text-red-400 border-red-900/50',
     medium: 'text-amber-400 border-amber-900/50',
     low:    'text-gray-500 border-gray-200 dark:border-gray-800',
   };
   ```
   Keep text-red-400 and text-amber-400 — they are semantic colors readable in both modes.

6. Tension alert container at line ~256:
   `bg-gray-900 border rounded-lg` → `bg-gray-50 dark:bg-gray-900 border rounded-lg`
   The severity border class is applied via SEVERITY_COLORS (already handled above).

7. Tension alert description paragraph: `text-gray-400` → `text-gray-600 dark:text-gray-400`

8. Stalled commitment date span: `text-gray-600` → `text-gray-500 dark:text-gray-600`

**src/app/settings/page.tsx:**

1. h1 `text-gray-200` → `text-gray-800 dark:text-gray-200`

2. Section h2 headers `text-gray-400` → `text-gray-600 dark:text-gray-400`

3. Type-row cards: `bg-gray-900 border border-gray-800` → `bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-800`

4. Row label text `text-gray-200` → `text-gray-800 dark:text-gray-200`

5. Row description text `text-gray-500` — leave as-is

6. Row id/badge text `text-gray-600` → `text-gray-500 dark:text-gray-600`

7. Agent config section card: same as type-row cards above

8. Config description `text-gray-500` — leave as-is

9. Config key labels `text-gray-500` — leave as-is

10. Config value text `text-gray-300` → `text-gray-700 dark:text-gray-300`

**src/app/capture/page.tsx:**

1. h1 `text-gray-200` → `text-gray-800 dark:text-gray-200`

2. h2 `text-gray-400` → `text-gray-600 dark:text-gray-400`

Do NOT touch handleSubmit, fetchNodes, or any non-UI code.
  </action>
  <verify>
    <automated>grep -n "dark:bg-gray-9\|dark:border-gray-8\|dark:text-gray-[248]" /Users/gurden/Documents/code/cof-learning-system/src/app/review/page.tsx /Users/gurden/Documents/code/cof-learning-system/src/app/settings/page.tsx /Users/gurden/Documents/code/cof-learning-system/src/app/capture/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "dark:bg-gray-9" src/app/review/page.tsx returns at least 2 matches (list cards)
    - grep "dark:border-gray-8" src/app/review/page.tsx returns at least 2 matches (card borders)
    - grep "dark:text-gray-2" src/app/review/page.tsx returns at least one match (h1)
    - grep "dark:bg-gray-9" src/app/settings/page.tsx returns at least 2 matches (type-row cards)
    - grep "dark:text-gray-2" src/app/settings/page.tsx returns at least one match (row labels)
    - grep "dark:text-gray-2\|dark:text-gray-4" src/app/capture/page.tsx returns at least 2 matches (h1 + h2)
    - grep "bg-gray-900" src/app/review/page.tsx — zero unadorned occurrences (all must have dark: prefix)
    - grep "bg-gray-900" src/app/settings/page.tsx — zero unadorned occurrences
  </acceptance_criteria>
  <done>Review, settings, and capture pages have no hardcoded dark-only gray classes. All gray backgrounds and text colors have light-mode equivalents.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add dark: variants to ReflectClient.tsx, ReviewCard.tsx, and InlineCaptureCard.tsx</name>
  <files>src/app/reflect/ReflectClient.tsx, src/components/review/ReviewCard.tsx, src/components/graph/InlineCaptureCard.tsx</files>
  <read_first>
    - /Users/gurden/Documents/code/cof-learning-system/src/app/reflect/ReflectClient.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/review/ReviewCard.tsx
    - /Users/gurden/Documents/code/cof-learning-system/src/components/graph/InlineCaptureCard.tsx
  </read_first>
  <action>
**src/app/reflect/ReflectClient.tsx:**

1. windowButtonClass inactive branch:
   `bg-gray-700 text-gray-300 hover:bg-gray-600`
   → `bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 hover:bg-gray-300 dark:hover:bg-gray-600`
   Leave the active branch (`bg-teal-600 text-white`) unchanged — semantic color.

2. Goal space trajectory cards (line 113):
   `bg-gray-800 rounded p-4`
   → `bg-gray-100 dark:bg-gray-800 rounded p-4`

3. Goal space title text (line 114): `text-gray-200` → `text-gray-800 dark:text-gray-200`

4. Score text (line 116): `text-gray-400` → `text-gray-600 dark:text-gray-400`

5. Section h2 headings `text-gray-100` — these are bold section titles. Update to:
   `text-gray-900 dark:text-gray-100`

6. Reflection question labels (line 131): `text-gray-300` → `text-gray-700 dark:text-gray-300`

7. Reflection textareas (line 133):
   `bg-gray-800 border border-gray-600 rounded p-2 text-gray-100`
   → `bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded p-2 text-gray-900 dark:text-gray-100`

8. Decision list items (line 148):
   `bg-gray-800 rounded p-3`
   → `bg-gray-100 dark:bg-gray-800 rounded p-3`

9. Decision item text (line 149): `text-gray-200` → `text-gray-800 dark:text-gray-200`

10. Decision input (line 166):
    `bg-gray-800 border border-gray-600 rounded p-2 text-sm text-gray-100`
    → `bg-gray-50 dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded p-2 text-sm text-gray-900 dark:text-gray-100`

11. Add Decision button (line 174):
    `bg-gray-700 text-gray-300 rounded hover:bg-gray-600`
    → `bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded hover:bg-gray-300 dark:hover:bg-gray-600`

Do NOT change the teal-600 Save button, text-green-400 success message, or text-red-400 error message — semantic colors.

**src/components/review/ReviewCard.tsx:**

1. "Suggested Connections" label (line 149): `text-gray-400` → `text-gray-600 dark:text-gray-400`

2. "No connections suggested" paragraph (line 163): `text-gray-600` → `text-gray-500 dark:text-gray-600`

3. Action divider (line 168): `border-t border-gray-800` → `border-t border-gray-200 dark:border-gray-800`

Do NOT change the Promote/Draft/Archive button classes — they use node-type semantic colors.

**src/components/graph/InlineCaptureCard.tsx:**

1. Card root div (line 131):
   `bg-gray-900 border border-gray-700 rounded-xl shadow-2xl p-4`
   → `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl shadow-2xl p-4`

2. "New node" label (line 135): `text-gray-500` — leave (same in both modes)

3. Title input (line 144):
   `bg-gray-800 border border-gray-700 rounded-md px-3 py-2 text-sm text-gray-200 placeholder-gray-600`
   → `bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-md px-3 py-2 text-sm text-gray-800 dark:text-gray-200 placeholder-gray-400 dark:placeholder-gray-600`
   focus class: `focus:border-gray-500` → `focus:border-gray-400 dark:focus:border-gray-500`

4. Node type select (line 150):
   `bg-gray-800 border border-gray-700 rounded-md px-3 py-1.5 text-xs text-gray-400`
   → `bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-md px-3 py-1.5 text-xs text-gray-600 dark:text-gray-400`

5. Goal space section containers (lines 158, 180):
   `bg-gray-800/50 border border-gray-700/50`
   → `bg-gray-100/50 dark:bg-gray-800/50 border border-gray-200/80 dark:border-gray-700/50`

6. "normal-case font-normal" helper text in goal space labels (the text-gray-600 span):
   `text-gray-600` → `text-gray-500 dark:text-gray-600`

7. Goal space select (line 168):
   `bg-gray-800 border border-gray-700 text-xs text-gray-300`
   → `bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 text-xs text-gray-700 dark:text-gray-300`
   focus class: `focus:border-gray-600` → `focus:border-gray-400 dark:focus:border-gray-600`

8. Outcome select (line 189) — same replacement as goal space select.

9. Expected signals input (line 207):
   `bg-gray-800 border border-gray-700 rounded-md px-3 py-2 text-sm text-gray-200 placeholder-gray-600`
   → `bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-md px-3 py-2 text-sm text-gray-800 dark:text-gray-200 placeholder-gray-400 dark:placeholder-gray-600`
   focus class: `focus:border-gray-500` → `focus:border-gray-400 dark:focus:border-gray-500`

10. Cancel button (line 222):
    `text-gray-500 hover:text-gray-300`
    → `text-gray-500 hover:text-gray-700 dark:hover:text-gray-300`

Do NOT change `bg-node-hunch` Create button, or any [#085041] label color.
  </action>
  <verify>
    <automated>grep -n "dark:bg-gray\|dark:border-gray\|dark:text-gray" /Users/gurden/Documents/code/cof-learning-system/src/app/reflect/ReflectClient.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/review/ReviewCard.tsx /Users/gurden/Documents/code/cof-learning-system/src/components/graph/InlineCaptureCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "dark:bg-gray-8" ReflectClient.tsx returns at least 3 matches (goal space cards, textareas, decision items, inputs)
    - grep "dark:border-gray-6" ReflectClient.tsx returns at least 2 matches (textarea + input borders)
    - grep "bg-gray-800" ReflectClient.tsx — zero unadorned occurrences (all must have dark: prefix)
    - grep "dark:border-gray-8" ReviewCard.tsx returns one match (action divider)
    - grep "dark:bg-gray-9" InlineCaptureCard.tsx returns one match (card root)
    - grep "dark:border-gray-7" InlineCaptureCard.tsx returns at least 3 matches (inputs, selects, containers)
    - grep "bg-gray-900" InlineCaptureCard.tsx — zero unadorned occurrences
    - grep "bg-gray-800" InlineCaptureCard.tsx — zero unadorned occurrences
  </acceptance_criteria>
  <done>ReflectClient trajectory cards, textareas, and inputs are readable in both light and dark mode. ReviewCard action divider has a light-mode border. InlineCaptureCard overlay card, all inputs, and all selects adapt cleanly to both modes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "bg-gray-900" src/app/review/page.tsx` — zero unadorned matches (all have dark: prefix)
2. `grep "bg-gray-900" src/app/settings/page.tsx` — zero unadorned matches
3. `grep "dark:text-gray-2" src/app/capture/page.tsx` — at least one match
4. `grep "bg-gray-800" src/app/reflect/ReflectClient.tsx` — zero unadorned matches
5. `grep "dark:border-gray-8" src/components/review/ReviewCard.tsx` — one match
6. `grep "bg-gray-900" src/components/graph/InlineCaptureCard.tsx` — zero unadorned matches
7. `grep "bg-gray-800" src/components/graph/InlineCaptureCard.tsx` — zero unadorned matches
</verification>

<success_criteria>
- src/app/review/page.tsx: all list-card backgrounds and text colors use light+dark pairs
- src/app/settings/page.tsx: all type-row cards use light+dark pairs
- src/app/capture/page.tsx: page and section headings use dark: text variants
- src/app/reflect/ReflectClient.tsx: all interactive elements (cards, textareas, inputs, buttons) use dark: variants
- src/components/review/ReviewCard.tsx: action divider border has dark: variant
- src/components/graph/InlineCaptureCard.tsx: card root, all inputs, all selects use dark: variants
- No file in this plan contains an unadorned bg-gray-900 or bg-gray-800 (not preceded by a light-mode class)
- LAYOUT-02 requirement is fully satisfied: no hardcoded dark-only colors remain anywhere in the app
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-theme/08-04-SUMMARY.md`
</output>
